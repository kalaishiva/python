# Use for loop to generate token number from 1 to 10 using range().
# print: "Serving chai to Token # {number}"

for token in range(1,11): # range are not inclusive so 1 to 11
    print(f" Serving chai to Token {token}")